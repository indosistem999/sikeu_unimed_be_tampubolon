{
  "SchemaCreateToMasterIdentity": {
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "description": "Enter name"
      },
      "file_logo": {
        "type": "string",
        "format": "base64",
        "description": "Upload image for module icon (opsional)"
      },
      "phone": {
        "type": "string",
        "description": "Enter phone (opsional)"
      },
      "email": {
        "type": "string",
        "description": "Enter email (opsional)"
      },
      "address": {
        "type": "string",
        "description": "Enter address (opsional)"
      },
      "website": {
        "type": "string",
        "description": "Enter website (opsional)"
      }
    }
  },
  "SchemaUpdateToMasterIdentity": {
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "description": "Enter name"
      },
      "file_logo": {
        "type": "string",
        "format": "base64",
        "description": "Upload image for module icon (opsional)"
      },
      "phone": {
        "type": "string",
        "description": "Enter phone (opsional)"
      },
      "email": {
        "type": "string",
        "description": "Enter email (opsional)"
      },
      "address": {
        "type": "string",
        "description": "Enter address (opsional)"
      },
      "website": {
        "type": "string",
        "description": "Enter website (opsional)"
      }
    }
  }
}
